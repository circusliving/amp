
version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/node:latest
    steps:
    - run: # test what branch we're on.
        name: "What CIRCLE_WORKING_DIRECTORY?"
        command: echo ${CIRCLE_WORKING_DIRECTORY}